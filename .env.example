# Local env for `task run` (gitignored). Copy: cp .env.example .env
# Same All-Inkl SMTP as Authelia for now; swap later as needed.

SHEJERA_SMTP_HOST=w00bb878.kasserver.com
SHEJERA_SMTP_PORT=587
SHEJERA_SMTP_USER=m080302c
SHEJERA_SMTP_FROM=o.homelab@karahan.de
SHEJERA_SMTP_STARTTLS=true
SHEJERA_SMTP_PASSWORD=

# Absolute invite links in emails (frontend or contrib origin)
SHEJERA_INVITE_ORIGIN=http://localhost:5173
