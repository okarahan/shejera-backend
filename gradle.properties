kotlin.code.style=official
org.gradle.jvmargs=-Xmx512m -Dfile.encoding=UTF-8
org.gradle.java.installations.auto-download=true

# jOOQ codegen + Flyway use Postgres on :5432 (`docker compose up` via Gradle if down).
jooq.url=jdbc:postgresql://localhost:5432/shejera
jooq.user=shejera
jooq.password=shejera
